[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @lifeos/api build"

[deploy]
startCommand = "pnpm --filter @lifeos/api start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
